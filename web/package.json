{
  "name": "livescorehd-web",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
  }
}
